spring:
  application:
    name: book-system


  datasource:
    url: jdbc:postgresql://localhost:5432/books
    username: kaua.novaes
    password: 131910
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true